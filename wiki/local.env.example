# Copy to local.env and fill in. local.env is gitignored.
WIKI_HOST=myserver                 # ssh alias or user@host
COBBLEMON_DIR=/srv/cobblemon       # server directory containing mods/ and world/
WIKI_SERVER_ADDR=play.example.com  # shown in the wiki header
